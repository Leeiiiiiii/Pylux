# Template: copy to secrets/ios/credentials.env (same directory; that file is not committed).
# Fill in values, then paste into GitHub → Settings → Secrets → Actions.
# Generate base64 values with: base64 -i <file> | pbcopy

APP_STORE_CONNECT_API_KEY_KEY_ID=
APP_STORE_CONNECT_API_KEY_ISSUER_ID=
APP_STORE_CONNECT_API_KEY_KEY_BASE64=
IOS_CERTIFICATE_P12_BASE64=
IOS_CERTIFICATE_PASSWORD=
IOS_PROVISIONING_PROFILE_BASE64=
